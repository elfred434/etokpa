export default '\n  function toggleSecret(id, rawVal, maskedVal, btn) {\n    const el = document.getElementById(id);\n    if (!el) return;\n    const isMasked = el.textContent === maskedVal;\n    if (isMasked) {\n      el.textContent = rawVal;\n      btn.innerHTML = \'<span class="material-symbols-outlined text-[16px] text-primary">visibility_off</span>\';\n    } else {\n      el.textContent = maskedVal;\n      btn.innerHTML = \'<span class="material-symbols-outlined text-[16px]">visibility</span>\';\n    }\n  }\n';
