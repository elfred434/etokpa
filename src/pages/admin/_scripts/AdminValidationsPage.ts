export default "\n        // Simple interactivity for demonstration\n        document.querySelectorAll('tbody tr').forEach(row => {\n            row.addEventListener('click', () => {\n                // Remove highlight from all\n                document.querySelectorAll('tbody tr').forEach(r => {\n                    r.classList.remove('bg-primary-tint', 'border-l-4', 'border-primary');\n                    r.classList.add('hover:bg-gray-50');\n                });\n                \n                // Add highlight to clicked\n                row.classList.add('bg-primary-tint', 'border-l-4', 'border-primary');\n                row.classList.remove('hover:bg-gray-50');\n                \n                // Micro-interaction: scale down briefly\n                row.style.transform = 'scale(0.995)';\n                setTimeout(() => row.style.transform = 'scale(1)', 100);\n            });\n        });\n\n        // Add ripple-like effect on buttons\n        const buttons = document.querySelectorAll('button');\n        buttons.forEach(btn => {\n            btn.addEventListener('mousedown', () => {\n                btn.style.transform = 'scale(0.97)';\n            });\n            btn.addEventListener('mouseup', () => {\n                btn.style.transform = 'scale(1)';\n            });\n            btn.addEventListener('mouseleave', () => {\n                btn.style.transform = 'scale(1)';\n            });\n        });\n    ";
