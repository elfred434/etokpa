// Même écran que /manager/parametres (les deux maquettes param_tres_manager_1/2 = deux états
// de la même page) — cette route garde l'URL historique du design.
export { default } from './ManagerParametresPage';
